<configuration>


    <!--TODO CONSIDER ADDING 
    INFO org.mongodb.driver.connection  -->

    <!-- for production please use /var/log/xyz directory instead of /tmp/log/xyz -->
    <!-- <property name="LOG_FILE" value="/tmp/log/woshiadai/server.log"/>
    <property name="REQUEST_LOG_FILE" value="/tmp/log/woshiadai/request.log"/> -->

    <property name="LOG_FILE" value="./log/server.log"/>
    <property name="DEBUG_FILE" value="./log/serverDebug.log"/>
    <!-- <property name="REQUEST_LOG_FILE" value="./log/request.log"/> -->
    <!-- console output -->
    <appender class="ch.qos.logback.core.ConsoleAppender" name="STDOUT">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- server log file output -->
    <appender name="SERVER_LOG" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE}</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_FILE}.%d{yyyy-MM-dd}.%i.gz</fileNamePattern>
            <maxFileSize>200MB</maxFileSize>
            <maxHistory>24</maxHistory>
            <totalSizeCap>5GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%-4relative [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    <appender name="SERVER_LOG_ASYNC" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="SERVER_LOG" />
        <discardingThreshold>0</discardingThreshold>
        <queueSize>512</queueSize>
        <maxFlushTime>5000</maxFlushTime>
        <neverBlock>true</neverBlock>
    </appender>

    <!-- request log output -->
    <appender name="DEBUG_LOG" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${DEBUG_FILE}</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${DEBUG_FILE}.%d{yyyy-MM-dd}.%i.gz</fileNamePattern>
            <maxFileSize>900MB</maxFileSize>
            <maxHistory>14</maxHistory>
            <totalSizeCap>2GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%-4relative [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    <appender name="DEBUG_LOG_ASYNC" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="DEBUG_LOG" />
        <discardingThreshold>0</discardingThreshold>
        <queueSize>5000</queueSize>
        <neverBlock>true</neverBlock>
    </appender>

    <!-- logger for application package -->
    <!-- TODO   -->
    <logger level="INFO" name="org.example" additivity="false">
        <appender-ref ref="SERVER_LOG_ASYNC"/>
    </logger>

    <logger level="DEBUG" name="userModule" additivity="false">
        <appender-ref ref="DEBUG_LOG_ASYNC"/>
    </logger>

    <logger level="DEBUG" name="org.example.repository" additivity="false">
        <appender-ref ref="DEBUG_LOG_ASYNC"/>
    </logger>

    <!-- logger for RAPS request log -->
    <!-- <logger level="INFO" name="io.woshiadai.starter.Slf4jRequestLogger" additivity="false">
        <appender-ref ref="REQUEST_LOG_ASYNC"/>
    </logger> -->

    <!-- root logger -->
    <root level="INFO">
        <appender-ref ref="STDOUT"/>
    </root>

</configuration>